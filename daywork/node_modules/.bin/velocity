#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_velocity@0.7.2@velocity/bin/velocity" "$@"
  ret=$?
else 
  node  "$basedir/../_velocity@0.7.2@velocity/bin/velocity" "$@"
  ret=$?
fi
exit $ret
